const mongoose = require('mongoose');
const History = require('../models/history');

const getHistory = async (rec, res, next) => {
    // console.log('getHistory');
};

const addNewHistory = async (rec, res, next) => {
    // console.log('addNewHistory');
};

const deleteHistory = async (rec, res, next) => {
    // console.log('deleteHistory');
};


module.exports = {
    getHistory,
    addNewHistory,
    deleteHistory,
    //...
}