const mongoose = require('mongoose');
const Therapy = require('../models/therapy');

const getTherapyForPatient = async (rec, res, next) => {
    // console.log('getTherapyForPatient');
};

const addNewTherapy = async (rec, res, next) => {
    // console.log('addNewTherapy');
};

module.exports = {
    getTherapyForPatient,
    addNewTherapy,
    // ...
}